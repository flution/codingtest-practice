using System;

public class Solution {
    public string solution(string[] arr) {
        string answer = "";

        foreach (string str in arr) {
            answer += str;
        }

        return answer;
    }
}
